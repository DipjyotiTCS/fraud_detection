# Local backend URL used by React at build/dev time.
# For local usage:
VITE_API_BASE_URL=http://localhost:8000

# For public tunnel usage, replace this with the backend tunnel URL, for example:
# VITE_API_BASE_URL=https://your-backend-tunnel.trycloudflare.com
